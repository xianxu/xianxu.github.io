---
layout: post
title:  'what is "navigate between splits"'
date:   2026-03-27
tags: unclassified
comments: true
---

<style>
h1 { color: #1a365d; border-bottom: 3px solid #4299e1; padding-bottom: 0.3rem; }
h2 { color: #2b6cb0; border-bottom: 2px solid #bee3f8; padding-bottom: 0.3rem; }
h3 { color: #3182ce; border-left: 4px solid #90cdf4; padding-left: 0.8rem; }
.branch-nav { margin: 1rem 0; padding: 0.6rem 1rem; border-radius: 8px; font-size: 0.95em; }
.branch-nav a { text-decoration: none; font-weight: 500; }
.branch-nav a:hover { text-decoration: underline; }
.branch-nav.parent-link { background: linear-gradient(135deg, #fefcbf 0%, #faf089 100%); border-left: 4px solid #d69e2e; color: #744210; }
.branch-nav.parent-link a { color: #975a16; }
.branch-nav.child-link { background: linear-gradient(135deg, #c6f6d5 0%, #9ae6b4 100%); border-left: 4px solid #38a169; color: #22543d; }
.branch-nav.child-link a { color: #276749; }
.branch-inline { color: #2b6cb0; text-decoration: none; border-bottom: 1px dashed #90cdf4; font-weight: 500; }
.branch-inline:hover { color: #1a365d; border-bottom-style: solid; }
</style>

This transcript is generated by [parley.nvim](https://github.com/xianxu/parley.nvim).


<div class="branch-nav parent-link" markdown="0"><a href="{% post_url 2026-03-26-ghostty_customization_options %}">← Ghostty customization options</a></div>


## Question

 what is "navigate between splits"?


🤖:[Claude-Code-Opus]

🧠: The user is asking about "navigate between splits" in the context of Ghostty. I mentioned this in passing or it's a concept they encountered. In Ghostty (and many terminal emulators), "splits" refer to splitting the terminal window into multiple panes — similar to tmux or iTerm2 splits. "Navigate between splits" means moving focus from one split pane to another. Let me explain this concisely since the user seems to be exploring Ghostty's features.

In Ghostty, **splits** are multiple terminal panes within a single window — you can divide your window horizontally or vertically, each running its own shell session (similar to tmux panes or iTerm2 splits).

**"Navigate between splits"** simply means moving your keyboard focus from one pane to another. Ghostty provides keybindings for this:

### Default Keybindings (macOS)

| Action | Default Keybinding |
|---|---|
| Split right (vertical) | `Cmd + D` |
| Split down (horizontal) | `Cmd + Shift + D` |
| Navigate to split above | `Ctrl + Alt + ↑` (or `Cmd + Alt + ↑` on macOS) |
| Navigate to split below | `Ctrl + Alt + ↓` |
| Navigate to split left | `Ctrl + Alt + ←` |
| Navigate to split right | `Ctrl + Alt + →` |
| Close split | `Cmd + W` |

### Why It Matters

Without split navigation keybindings, you'd have to click with your mouse to focus a different pane. The keybindings let you stay fully keyboard-driven.

This is also where `unfocused-split-opacity` becomes relevant — it visually dims the panes you're *not* focused on, making it obvious which split is active as you navigate between them.

You can customize all of these keybindings in your Ghostty config file using the `keybind` directive, e.g.:

```
keybind = ctrl+h=goto_split:left
keybind = ctrl+j=goto_split:bottom
keybind = ctrl+k=goto_split:top
keybind = ctrl+l=goto_split:right
```

These vim-style bindings (`h/j/k/l`) are a popular customization.

📝: You asked what "navigate between splits" means in Ghostty, I answered that splits are multiple terminal panes in one window, and navigating between them means moving keyboard focus from one pane to another using directional keybindings, with examples of defaults and vim-style custom bindings.

## Question


